# ===== 敏感信息（必须排除）=====
.env
.env.*
*.env
.envrc

# API Key / Token / 凭据
*.key
*.pem
*.p12
credentials.json
secrets.json
**/secrets/
**/credentials/

# 个人隐私
*.log
*.bak
*~
*.swp
.DS_Store
Thumbs.db

# ===== Python =====
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/
.python-version

# ===== WorkBuddy 工具运行时数据 =====
# 2026-09-17：项目级 skill（.workbuddy/skills/<name>/）是刻意入库的共享内容，
# 但逐轮工作日志属于运行时数据，不入库。
# 修正记录：原先写的是 `.workbuddy/skills/.workbuddy/` 整目录忽略 —— 当时工作区
# 被设成了 `<repo>/.workbuddy/skills`，导致 skill 真实落点变成嵌套的
# `.workbuddy/skills/.workbuddy/skills/<name>/`，被这条规则连带挡在版本库外
# （能用的进不了库、进了库的不生效）。现已把工作区指回仓库根、skill 迁到
# `.workbuddy/skills/<name>/`，忽略规则收窄为只挡 memory。
.workbuddy/memory/

# ===== Node / VS Code 临时 =====
node_modules/
# 2026-09-17 修正：改为整目录忽略。原先只挡了 launch.json / tasks.json，
# 但 settings.json 里存的是本机绝对路径（C:\Users\19623\...），提交上去别人 clone 没有意义。
.vscode/
.history/

# ===== 大数据/数据文件（仓库不该存原始数据）=====
*.csv
*.tsv
*.parquet
*.arrow
*.h5
*.hdf5
*.pkl
*.pickle
data/raw/
data/interim/

# ---- 例外：课程配套的示例数据与产出物（几十 KB 的小样本，需要入库）----
# 2026-09-17 加。`*.csv` 这条原意是「不把原始大数据塞进仓库」，但它是无差别的通配，
# 会连带挡掉第 2 课产出物「干净语料.csv」和课上用的标注/评论数据 ——
# 做完的东西被静默忽略、`git status` 还显示干净，与之前 `*.ipynb` 是同一类坑。
# 注意：gitignore 的 `!` 例外**不能救回被排除目录里的文件**，这里排除的是文件本身，所以有效。
!.workbuddy/skills/python-ai-4lessons/**/*.csv
!.workbuddy/skills/python-ai-4lessons/**/*.tsv

# ===== Jupyter =====
.ipynb_checkpoints/
# 2026-09-17 修正：原先这里有 `*.ipynb`，把 notebook 全挡住了。
# 但 .ipynb 是本课程作业的交付物（scripts/01.ipynb 等），必须入库，故删除该规则。

# ===== OS =====
.DS_Store
desktop.ini

# ===== 打包产物 =====
*.zip
*.tar
*.tar.gz
*.tgz
*.7z
*.rar
dist/
build/
